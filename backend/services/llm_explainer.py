# def summarize_with_llm(factors: list[str]) -> Optional[str]:
#     """
#     OPTIONAL: use LLM to summarize rule-based factors.
#     Replace with real LLM call if needed.
#     """
#     if not factors:
#         return None

#     # MOCK (можеш замінити OpenAI / local LLM)
#     return (
#         "The molecule exhibits several characteristics commonly associated with cardiotoxicity, "
#         + "including " + ", ".join(factors[:2]).lower() + "."
#     )
